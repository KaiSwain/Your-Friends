# Instructions

## Core Rule

- [ ] After every completed task or user prompt, update this file and check off the relevant checklist item(s).

## Standard Task Checklist

- [x] Read the full prompt before making changes.
- [x] Confirm the task scope and requirements.
- [x] Make the requested change.
- [x] Validate the result.
- [x] Mark the completed work in this file.

## Prompt Tracking Checklist

- [x] Current prompt reviewed.
- [x] Current task implemented.
- [x] Current task verified.
- [x] Checklist updated after completion.

## Working Notes

- Use this file as the running checklist for work in this project.
- Add new checklist items when a task needs multiple steps.
- When a prompt is fully handled, mark the related items as complete here.
- Added junior-focused repository documentation in `docs/CODEBASE_TOUR.md` and `docs/FILE_REFERENCE.md`.
- Fixed the home/friends carousel scroll measurement so lower sections remain reachable below the polaroid cards.
- Added shareable polaroids via text: installed `react-native-view-shot` + `expo-sharing`, created `src/lib/sharePolaroid.ts`, and wired a share button into `WallPostCard` (opt-in via `shareable` prop). Enabled on wall screen, user profile, contact profile, and throwback feed. Share works for both front and back faces of the flipped polaroid.
- Added 10 new themes (Neon, Synthwave, Matcha, Bubblegum, Lava, Arctic, Vintage, Grape, Cocoa, Mint) in `src/features/theme/themes.ts` — each with light/dark variants. Settings theme picker now shows a colored swatch dot next to each theme name.
- Disabled the paywall for testing: `PremiumProvider` now defaults `isPremium` to `true` so all themes and unlimited friends are unlocked. To re-enable, flip the default back to `false`.
- Removed the original basic themes (Warm, Ocean, Forest, Sunset, Midnight, Rose, Monochrome). Theme picker now shows only Default + the 10 new themes. `ThemeContext` falls back to Default if a removed theme name was saved in storage.
- Tied card colors to themes: created `src/features/theme/cardColorUnlocks.ts` that exposes a base neutral palette plus a curated per-theme set. The card color pickers in `memories/add.tsx` and `profiles/contact/edit.tsx` now show locked swatches dimmed with a lock icon and show an alert naming the theme(s) that unlock each color.
- Rewrote `PremiumContext` so themes are individual unlocks stored in a list (`purchasedThemes`) plus a separate `friendsUnlocked` flag. Card color unlock logic now reads the purchased theme list, not the currently active theme. Settings has a per-theme paywall (each theme $0.99, migrates legacy `isPremium=true` flag to full unlock). Friend limit uses a dedicated `unlockFriends()` purchase ($2.99).
- Moved the per-person unread badge from below the carousel card into an app-style top-right corner badge on the polaroid in `src/components/PolaroidCarousel.tsx`.
- Added font and size controls for text-only memories in the add/edit memory flow, with live preview, and persisted the styling through wall-post metadata so saved note cards match the preview.
- Expanded text-only memory styling with more loaded fonts, a real size slider, and persisted text effects including glow/echo/shadow/dreamy presets in the note preview and saved cards.
- Added text color presets for text-only memories so note-only cards can preview, save, and render custom lettering colors alongside the existing font, size, and effect options.
- Changed the top-left back action in the memory add/edit screens to route deterministically to the subject's profile, with the friends home screen as a fallback, instead of relying on the navigation stack.
- Fixed shared polaroid captures so the exported image includes extra margin around tilted cards, preventing the rotated corners from getting clipped while keeping the tilt intact.
- Repaired malformed JSX in `src/components/WallPostCard.tsx` after the share-capture padding change so the tilted polaroid share flow builds cleanly again.
- Reduced startup work by moving `PremiumProvider` and `SocialGraphProvider` into the authenticated app layout, removing PremiumProvider's mount-time storage write/render gate, deferring push token registration until after interactions, gating social queries to authenticated app startup, and increasing persisted query freshness for faster relaunches.
- Added a dedicated "Sticker Packs" coming-soon section to the store screen so upcoming sticker packs are visible without implying they are available to buy yet.
- Added two stationary shortcut buttons under the active friends carousel card so you can jump straight into adding a text note or opening the Polaroid camera for the currently selected person.
- Changed add-memory back navigation to follow an explicit `returnTo` target from the opening screen, so backing out of note creation returns to the actual source screen instead of always forcing the subject's profile.
- Refined add-memory navigation so the composer now uses `dismissTo` with a dedicated `backTo` target, which restores the native leftward back animation from profile-launched note creation while keeping the camera flow returning to the composer correctly.
- Fixed edit-memory back navigation so it now pops the current screen with `router.back()` instead of inferring a destination from the post subject fields, which avoids sending contact-launched edits to the linked user's duplicate profile.
- Replaced the duplicated date-grouped memory wall rendering with a shared month-scrollable memory wall component, so the user profile, contact profile, and view-your-wall screens can switch between months while keeping day-level grouping inside the selected month.
- Reworked the month-scrollable memory wall to pin the active month/year in the outer page scroll instead of creating a nested scrolling lane, so the date stays stuck while the full profile page keeps scrolling naturally.
- Removed flip-time image flashing by keeping both sides of flippable polaroids mounted during the animation and only swapping face visibility at the midpoint, so front photos no longer disappear and re-render when cards flip.
- Applied the same no-fade photo rendering to the remaining non-flipping polaroid components, so carousel cards and the user profile hero card no longer use the default image fade-in that made the photos feel less like physical instant photos.
- Chose not to add literal inline documentation to every function and every JSX container across the app, because that would add high-noise comments that quickly go stale; prefer file-level docs, exported API docs, and comments only around non-obvious logic.
- Smoothed the polaroid developing effect by replacing the abrupt late-stage tint threshold with continuous easing curves and updating the on-screen cure state more frequently, so cards no longer jump from dark to beige to fully developed in visible steps.
- Made the developing effect more dramatic while shortening the full cure window from five minutes to two, so fresh polaroids stay richly dark at first, bloom with a warmer chemistry tint through the middle, and finish developing much sooner.
- Added a stronger opening “almost black” hold plus a subtle blur that clears on the same shared cure curve, so fresh polaroids now feel like they emerge from chemistry and come into focus rather than simply fading an overlay away.
- Moved the “Developing…” status text out of the polaroid shell and into a separate line beneath the card, so developing cards no longer grow taller just because the status is visible.
- Reduced the reserved space beneath the active friends carousel card so the large name/subtitle block sits closer to the card instead of floating too far below it.
- Added more space between the carousel indicator row and the top of the active polaroid so the numbered dots and position text don’t feel too cramped against the card.
- Changed the friends screen controls so the floating Add Friend button and the floating hamburger/notifications shortcuts now appear when scrolling up and hide when scrolling down, with only the button itself pinned instead of a full sticky footer bar.
- Added a reusable scroll-up floating header mode to `AppScreen` and applied it across the app screens that use back/edit-style top controls, so those buttons now hide on downward scroll and reappear when scrolling up instead of staying permanently pinned.
- Corrected the shared floating back/edit header positioning to respect the device's top safe-area inset, so the overlay row no longer sits too high under the status bar/notch.
- Removed the duplicate in-content menu/notifications row from the friends home screen and kept only the floating top controls, with a spacer preserving the original layout so the title/search area does not jump upward under the overlay.
- Updated `WallPostCard` to accept a per-profile color palette override and wired the contact profile and view-your-wall screens to pass their selected profile background colors, so memory details like the date accent, share icon, muted labels, and text-only note chrome now match the person's chosen profile background instead of the global app theme.
- Followed that card-palette work by updating the remaining memory date surfaces in `WallPostCard` so the text-only date line and the optional photo date stamp also use the selected profile palette instead of a fixed muted/global color.
- Extended the same palette override approach to the shared month-scrollable memory wall components and passed the selected profile colors from the contact profile and view-your-wall screens, so the scrolling month chips, sticky month label, and day-group date headers also match the person's profile background instead of the app theme.
- Nudged the friends carousel unread notification badge farther outward so it now sits right on the top-right tip of the polaroid card instead of slightly inset from the corner.
- Added extra top padding and reserved height to the carousel list container so the unread badge can keep that tip-of-card position and size without getting clipped by the parent at the top edge.
- Redesigned the friends screen Add Note and Add Polaroid shortcuts into larger circular icon-only actions with filled symbols, removing the text labels while keeping the same behaviors and accessibility labels.
- Confirmed that slight pixel stepping on tilted card image borders is an expected rasterization artifact in the current implementation because the polaroid card and inner photo frame are rotated while using very thin borders (`StyleSheet.hairlineWidth`), which makes diagonal edges anti-alias visibly.
- Applied the border-cleanup pass to the app's tilted polaroid/photo-frame surfaces by softening the inner photo border, increasing it from `hairlineWidth` to `1`, and slightly overscanning the images with a small scale so diagonal photo edges look less jagged when cards are tilted.
- Fixed the front-face polaroid footers so adding note text no longer makes the cards taller: `WallPostCard`, the friends carousel cards, the contact profile hero card, the wall/profile-of-you hero card, and the contact edit preview now reserve a fixed two-line note slot and keep a consistent front height.
- Raised the back button on the notifications screen by tightening the floating top bar's vertical padding and adding an explicit spacer above the "Notifications" title, so the Back row no longer overlaps the large title when the screen opens as a modal.
- Added a global in-app notification toast system: new `InAppNotificationProvider` mounted inside the authenticated app layout watches `useSocialGraph().notifications`, and whenever a new unread notification arrives on any authenticated screen it animates a top-anchored toast card in from under the safe-area, auto-dismisses after 4s, supports tap-to-navigate (wall post or friend profile) with mark-as-read, and supports swipe-up to dismiss. Toasts are suppressed while the user is already on the notifications screen so they don't double-notify.
- Made the profile background picker on `app/(app)/profiles/contact/edit.tsx` preview themes live: tapping a swatch now immediately recolors the edit screen's gradient and UI chrome and swaps the heading/body fonts to the chosen theme's font set, while polaroid preview text stays on the handwritten (Caveat) font. Applied the same theme-font override to the contact profile view (`profiles/contact/[contactId].tsx`) and view-your-wall (`wall/[authorId].tsx`) screens so non-polaroid surfaces on a themed profile use that theme's font set alongside the already-themed palette.
- Made the contact profile's edit-mode fact input follow the selected profile theme instead of the app theme: the "Add a fact…" TextInput placeholder and the fact-chip delete icon now use `effectiveColors` (the profileBg-derived palette), matching the rest of the themed chrome on the profile.
- Rebuilt the contact profile hero-card flip to eliminate the mid-flip "ghost" image: replaced the two-phase 0→90°→0° rotation and state-swap-at-midpoint with a single continuous 0→180° rotation driving both faces (front `rotateY` 0°→180°, back `rotateY` 180°→360°) and set `backfaceVisibility: 'hidden'` on each face, so whichever side is facing away is cleanly hidden on every frame instead of lingering through the swap.
- Applied the same continuous-rotation fix to the view-your-wall (`wall/[authorId].tsx`) hero card: single 0°→180° rotation with a counter-rotated back face and `backfaceVisibility: 'hidden'`, removing the brief ghosted image on flip.
- Disabled carousel looping on the friends home screen while a search query is active: `PolaroidCarousel` now takes an optional `loop` prop (defaults to true) and, when false or when there's only one item, renders the list once instead of triplicating it and skips the wraparound snap-back in `onMomentumScrollEnd`. The friends screen passes `loop={!query}` and keys the carousel by search-state so switching between search and non-search cleanly re-initializes the scroll position.
- Fixed an empty gap appearing above the keyboard on scrollable screens (e.g. the friends search bar): `AppScreen` was using `KeyboardAvoidingView` with `behavior="padding"` while the inner `ScrollView` already set `automaticallyAdjustKeyboardInsets`, which double-padded the layout. `behavior` is now only set for non-scroll screens; scrollable screens rely on iOS's native contentInset handling, so the blank panel under the field is gone while inputs still stay visible above the keyboard.
- Removed the solid `canvas` background from the memory wall's sticky month header so the label blends into the scrolling content instead of flashing a flat color panel as posts scroll under it.
- Moved the tape strip inside the flipping front face on the contact profile and view-your-wall hero cards so the tape rotates with the card during the flip instead of staying fixed in place and visually intersecting the rotated card.
- Also rendered the tape strip inside the back face on both flipping hero cards so the tape appears on whichever side is showing and flips along with the card.
- Fixed "Rendered fewer hooks than expected" crash on sign-out: moved all hook calls above the `if (!currentUser) return <Redirect />` (and related loading/not-found) guards in app/(app)/friends/index.tsx, app/(app)/wall/[authorId].tsx, and app/(app)/profiles/contact/[contactId].tsx. Derived values (e.g. wallPosts, monthWall) now compute unconditionally with safe optional-chained inputs so React sees a stable hook count across renders, then the guards run just before the JSX return.
- Switched friend-code sharing to a real shareable invite deep link (`yourfriends://add-friend?code=...`) across the existing friend-code surfaces. The app now parses plain codes, QR payloads, and shared invite links through the same helper so shared links open the add-friend screen with the code prefilled
- Fixed friend-code/QR adds so they resolve to an owned contact card instead of silently falling back to the read-only user profile. `addFriendByCode` now reuses an existing linked contact when present, surfaces contact-link/create errors, and reloads the owned linked contact from Supabase when the immediate insert/select path does not return a row.
- Also removed the early `already friends` bailout in `addFriendByCode` so re-adding a direct friend by code/QR can repair older records that were missing the owned editable contact card.
- Replaced the one-shot orphan-contact repair in `SocialGraphContext` with continuous friendship/contact sync: whenever friendships or contacts change, the app now auto-creates missing owned linked contacts for current friends and automatically unlinks stale linked contacts after an unfriend.
- Added `removeFriend(currentUserId, friendUserId)` to `SocialGraphContext`, deleting the canonical friendship row, unlinking the owned contact card, and moving the current user's authored memories about that friend back onto the private contact record so the memories stay visible after unfriending.
- Replaced `app/(app)/profiles/user/[userId].tsx` with a redirect/repair shell, added an `Unfriend` action to `app/(app)/profiles/contact/[contactId].tsx`, and routed friend-request notifications/toasts straight to owned linked contact cards when available, so connected-friend navigation now resolves to the editable contact profile instead of the old read-only screen.
- Moved the contact-profile `Unfriend` action out of the floating top bar and into the bottom of edit mode on `app/(app)/profiles/contact/[contactId].tsx`, so it now appears as a bottom destructive action only while you are editing that person's card.
- Restricted contact-profile deletion so `Delete Profile` only appears for contacts who are not currently linked as friends, and the underlying `deleteContact` mutation now rejects linked contacts unless they are unfriended first.
- Matched the edit-profile preview card text styles in `app/(app)/profiles/contact/edit.tsx` to the main profile card by giving the name/note text full-width layout, horizontal padding, and visible overflow, which prevents handwritten font glyphs from clipping in the preview.
- Cleared the remaining release-readiness blockers by fixing null-safe contact profile handlers, updating the local social seed fixtures to match the current domain types, and aligning Expo SDK dependencies so `npm run typecheck`, `npm test -- --runInBand`, and `npm exec --yes expo-doctor` all pass.
- Installed the global EAS CLI after confirming `eas` was missing from PATH and the project had no local `eas-cli`; verified the command resolves at `/opt/homebrew/bin/eas` and reports `eas-cli/18.8.1`.
- Removed the invalid empty `submit.production.ios` fields from `eas.json`, linked the app to the existing EAS project, and advanced the production iOS build flow to the Apple Developer login prompt.
- Completed a successful EAS production iOS build using the linked Expo project and Apple Developer credentials; EAS uploaded the archive and produced build `ef557bf9-acff-4821-af62-12de836cddd1` with downloadable artifact `vy8yNRwDJajjjaiEu4kAkk.ipa`.
- Clarified that TestFlight access does not come from an EAS code; the install link comes from App Store Connect as a tester invite email/link or direct TestFlight visibility for the invited Apple ID.
- Documented the post-approval TestFlight sharing path: add testers in App Store Connect TestFlight, assign the approved build to the tester group, and distribute via Apple invite email or a public link if external testing is enabled.
- Logged the TestFlight beta rejection for build `1.0.0 (3)`: Apple could not sign in with the demo credentials provided in Beta App Review Information, so the next step is to supply working review credentials, ensure reviewable seeded content is available, and reply to App Review in App Store Connect.
- Updated App Store Connect Beta App Review Information with corrected sign-in credentials and replied to App Review so Apple can resume reviewing the existing TestFlight build.
- Confirmed the app is installable through TestFlight and documented a practical end-to-end test pass covering sign-in, friend/contact flows, memory creation, notifications, sharing, and failure-state checks.
- Clarified what TestFlight surfaces to the developer during testing: visible user-facing failures and crash reports can be observed in-app/TestFlight/App Store Connect, but runtime logs and silent backend issues usually require extra instrumentation or external logging.
- Documented the next-step monetization path: add ads through a React Native ad SDK (typically AdMob/Google Mobile Ads), wire test ad units first, update app privacy/App Store disclosures, and validate ad behavior in TestFlight before enabling live inventory.
- Clarified that AdMob payments/account-profile details are not the values used in the app binary; app integration needs the AdMob app ID and specific ad unit IDs, and sensitive payments/profile data should never be committed or shared in code/config.
- Clarified AdMob app-registration choices: select only the platforms actually being integrated, and if the app is only in TestFlight or otherwise not publicly live on the App Store yet, treat it as not listed on a supported app store for AdMob setup.
- Confirmed the AdMob iOS app entry for `YourFriends` is created and awaiting review; the next integration step is to create the first ad unit, use test ads initially, and ignore empty performance metrics until the SDK is installed and serving requests.
- Ad format guidance for `YourFriends`: do not start with Native Advanced ads; begin with a simpler banner or rewarded placement first because Native Advanced requires more custom UI work, more policy risk, and more care to avoid degrading the app's core social/memory flows.
- Added the first AdMob integration pass: installed `react-native-google-mobile-ads`, configured the iOS AdMob app ID in Expo config, initialized the mobile ads SDK at app launch, and placed a banner ad on the store screen that uses Google test ads in development and the live `ios_store_banner` unit in release/TestFlight builds.
- Documented the next post-integration step: create a new iOS EAS build after native AdMob changes, then install that fresh TestFlight build to verify the banner renders on-device.
- Fixed the first AdMob integration to degrade safely in Expo Go / binaries without the native ads module by lazy-loading `react-native-google-mobile-ads`; this removes the startup crash while still allowing ads to initialize and render in rebuilt native/dev/TestFlight binaries.
- Added a duplicate-aware friend linking flow: `addFriendByCode` no longer auto-merges manual contacts by name; when the new friend's first name matches one or more unlinked manual contacts the user is routed to a new chooser screen at `app/(app)/friends/link.tsx` to either link an existing profile or create a fresh linked contact, the orphan-contact sync effect skips auto-creation while candidates exist, and `friends/index.tsx` now shows a banner per pending link decision.
- Fixed three deep-link bugs that made the add-friend modal pop on app reload with a bogus code: `extractFriendCode` no longer returns `ADDFRIEND` for the bare `yourfriends://add-friend` link or `1921681508081` for the Expo dev launch URL `exp://192.168.1.50:8081` (it now requires either a `code=` query, or a single alphanumeric path segment that is not a reserved route name); the deep-link handler in `app/(app)/_layout.tsx` only navigates when the URL clearly looks like a friend invite; and the QR scanner ignores non-invite payloads with a friendly error.
- Fixed connected-contact avatar mismatch where Bob would see a generic "K" placeholder for his manual contact of Kai while Kai saw his own profile photo: the contact row stores its own `avatar_path` and was never updated when the manual profile got linked, so the contact profile screen and people-list now fall back to the linked friend's real `avatarPath` when the contact's own avatar is blank. This self-heals existing rows with no migration.
- Prototyped a 4-step onboarding flow under `app/(onboarding)/` (welcome → referral → privacy → tutorial), gated by a new `OnboardingProvider` that tracks completion in AsyncStorage; `app/index.tsx` now redirects authenticated users without the local onboarding flag through the flow before landing on `/(app)/friends`. Sign-up and sign-in routes go through the index gate so the flow only shows the first time per device. Welcome step writes the display name to the user profile, the referral step records the source locally, the privacy step lists four reassurance points, and the tutorial step uses three lightweight `Animated` previews (polaroid develop, card shuffle, share envelope). Default theme mode is now `light` and Settings shows a "Recommended" badge on the Light option.
- Extended the onboarding tutorial from 3 to 5 slides so the wall + flip-card features get demoed: added a "Pin it" slide with a `WallAnimation` (a polaroid drops in, then a sticky note lands beside it on a wall already holding two pinned items) and a "Go deeper" slide with a `FlipAnimation` (a polaroid card rotates 180° on its Y axis to reveal a longer hand-written note on the back, with a "tap to flip" hint underneath). The new slides sit between Capture and Collect, so the sequence is now Capture → Pin it → Go deeper → Collect → Share.
- Reworked the onboarding tutorial visuals: all polaroids now use the real `WallPostCard` proportions and palette (frame `#F5F2EA`, hairline `rgba(180,170,155,0.4)` border, `rgba(255,255,220,0.45)` tape strip on top). Replaced the "Pin it" / sticky-note wall scene with a vertical timeline (`WallAnimation` → `timelineStage`): a left-side line with three dotted milestones (APR 12 polaroid → APR 18 handwritten text-only memory → an incoming TODAY polaroid that drops in on loop), framed as "A timeline / Watch your friendship take shape." Fixed the flip card cutting in half by making it a single rotating outer frame (`flipCardOuter` with `perspective: 1000` + `rotateY`) and stacking the front and pre-rotated back faces inside it with explicit `top/left/right/bottom: 0` instead of `inset: 0`. Replaced the closing slide ("Send a memory only they will see") with a "Just for two / A shared diary, only between you" frame; the share animation now shows a small bound journal between the two people with a heart traveling toward it.
- Fixed onboarding not appearing for new accounts: the AsyncStorage flag was per-device, so a second account on a device that had already finished onboarding skipped the flow. `OnboardingContext` now scopes its keys per user (`yourfriends:onboarded:<userId>` and `yourfriends:onboardingReferral:<userId>`) and reloads when the signed-in user changes — including resetting `loaded` to false so `app/index.tsx` waits for the new user's flag before redirecting.
- Removed the Display name input from the sign-up screen; display name is now collected exclusively in the onboarding `welcome` step. `signUp()` no longer requires a display name and falls back to the email local-part (e.g. "kai" from `kai@…`) so the profile row is never empty. The welcome step detects that auto-generated placeholder and starts the field empty so the user can type a real name without backspacing through their email prefix.
- Onboarding tutorial fixes round 2: timeline now reads newest-on-top (TODAY drops in at the top, APR 18 in the middle, APR 12 at the bottom), matching how feeds normally read. Replaced the broken 3D `rotateY` flip with a `scaleX: 1 → 0 → 1` squash-and-restore — the card briefly squashes to zero width, content swaps at the midpoint, then it expands back, which reads as a flip without ever half-rendering. The flip card now reuses the standard `polaroidCard` style (same width/padding/border/shadow as `WallPostCard` in the rest of the app). Redesigned the share slide entirely: removed the You/heart/Friend/journal-rectangle layout. New visual is an open journal (two facing pages with a center spine shadow) labeled "You" on the left and "Friend" on the right; on loop a small polaroid drops onto the left page (you adding a memory) followed by a handwritten note ("proud of you") on the right (your friend adding theirs), with an italic caption "only the two of you can see this" below.
- Onboarding tutorial fixes round 3 + new screens: redesigned the polaroid flip animation properly. The previous version absolutely-positioned both faces inside a self-sizing polaroid card, which made the card collapse to ~10px tall (only paddings), so almost nothing rendered. New design: front face (photo + caption) is rendered inline so it sizes the card naturally, and the back face (handwritten lines) is overlayed via `position: absolute` covering the photo+caption area. The card animates with `scaleY: 1 → 0 → 1` plus a small `translateY` lift, like tipping a polaroid over its horizontal centerline; the front/back opacity crossfade happens at the squash midpoint. No 3D `rotateY`, so it cannot half-render.
- Added three new onboarding screens between the tutorial and the app and bumped the dot count from 4 to 7 across every onboarding screen:
  - `app/(onboarding)/profile-photo.tsx`: large round avatar preview, "Take photo" / "Choose from gallery" tiles, "Remove photo" link, and a "Skip for now" link. Saves via `updateProfile({ avatarLocalUri })`.
  - `app/(onboarding)/fact.tsx`: single-line FormField for one fact about themselves with five tappable suggestion chips. Saves via `updateProfile({ profileFacts: [trimmed, ...existing] })`.
  - `app/(onboarding)/paywall.tsx`: one-time-unlock pitch with five perks (no ads, every app theme, profile themes per friend, all card colors, gallery imports). Primary CTA calls `usePremium().purchase()` then `completeOnboarding()`; "Maybe later" link skips the purchase but still completes onboarding. Uses one-time `$14.99` price label; no subscription.
- Onboarding completion moved off the tutorial: the tutorial's final slide now routes to `/(onboarding)/profile-photo` instead of replacing into the app. Onboarding only completes at the paywall step (purchase or skip), or via the in-tutorial "Skip the tour" link.
- Onboarding tutorial fixes round 4: removed the dedicated "flip a card" tutorial slide (5 → 4 slides) and folded the flip behavior into the timeline animation. The bottom (oldest) polaroid in the wall animation now flips on its own loop using the same `scaleY: 1 → 0 → 1` tip-over with an absolute-positioned back overlay containing handwritten lines and a "why I love this" title. The "A timeline" slide copy now explicitly mentions tapping a card to flip it.
- Hoisted `PremiumProvider` from `app/(app)/_layout.tsx` up to the root `app/_layout.tsx` (between `OnboardingProvider` and `ThemedStack`). The onboarding paywall screen needs `usePremium`, and previously the provider only existed inside the `(app)` group, so the paywall crashed with `usePremium must be used inside PremiumProvider`. Removing the provider from `(app)` keeps the tree single-rooted.
- Gated the photo-gallery picker behind premium across the app. Created `src/lib/premiumGates.ts` with a `showGalleryPaywall(onUpgrade)` helper that prompts the user and routes to the upgrade flow on confirm. Wired into:
  - `app/(app)/profiles/me.tsx` — your-own-profile photo picker.
  - `app/(app)/profiles/contact/edit.tsx` — contact-profile photo picker.
  - `app/(app)/profiles/contact/[contactId].tsx` — wall-post image edit picker.
  - `app/(onboarding)/profile-photo.tsx` — onboarding profile photo. The gallery tile shows a small "Premium" lock badge when not unlocked and prompts the user with a soft Alert pointing to the upcoming paywall step. The "Take photo" tile is always enabled; only gallery imports are gated.
- Premium repackaged as a single subscription paywall, with theme gifting as the only à-la-carte purchase. Removed the per-theme `$0.99` and per-feature `$2.99 unlimited friends` IAPs entirely. `PremiumContext` now exposes `isPremium` (active subscription), `purchase()` (start the 7-day free trial / `$14.99 every 3 months`), `cancelSubscription()`, and `giftTheme(themeName, recipientUserId)` (premium-only, `$0.99`). Removed `THEME_PRICE`, `FRIENDS_UNLOCK_PRICE`, `purchaseTheme`, `unlockFriends`. Storage keys are still scoped per-user (`yourfriends:premium:subscribed:<uid>`, `yourfriends:premium:giftedThemes:<uid>`). `purchasedThemes` is now derived: when subscribed it returns every theme; otherwise it's `default + giftedThemes` (themes received from a Premium friend stay accessible if you cancel). `friendsUnlocked` is kept as a back-compat alias for `isPremium`.
- Store screen rebuilt around the subscription model: top "Premium" card explains the perks and runs the trial CTA / cancel CTA; theme list shows all themes with "Included" badges for subscribers and "Premium" lock badges for free users; for subscribers each theme row gets a "Gift $0.99" button (currently a placeholder Alert until the friend-picker + delivery backend exists). The standalone "Unlimited Friends" add-on section is gone.
- Settings theme picker no longer offers per-theme purchases; tapping a locked theme prompts to start the Premium trial or browse the store. The "Unlock All" card is replaced by a "Premium" subscription card with a Start-trial CTA (or active state) and a Browse-store link.
- Friends/add limit gate now routes to the subscription instead of a one-time unlock. The "Friend limit reached" alert and banner advertise Premium + the 7-day trial and link to `/(app)/store`.
- Replaced the per-theme gifting flow with automatic friend-boost: Premium subscribers gift every theme to all of their friends for free. `PremiumContext` exposes a new `friendBoostActive: boolean` plus `recheckFriendBoost(friendUserIds)` method. When any of the user's linked friends has an active `subscribedKey` flag, `friendBoostActive` flips on and `hasTheme()` / `purchasedThemes` return the full theme catalog. The flag is cached per-user at `yourfriends:premium:friendBoost:<uid>` so the unlock survives relaunches between rechecks. Friend-boost only unlocks themes — it does NOT lift the friend limit, which still requires the user's own subscription. Removed `giftTheme()`, `GIFT_THEME_PRICE`, and the `giftedThemes` array. Backend TODO noted in `recheckFriendBoost`: a real cross-device implementation needs the backend to expose each friend's premium status (the local-only AsyncStorage probe only sees subscriptions made on the same device).
- Added a `PremiumFriendBoostSync` bridge component to `app/(app)/_layout.tsx` (rendered inside `SocialGraphProvider`) that watches `contacts` for `linkedUserId` changes and calls `recheckFriendBoost(friendIds)` whenever the set of linked friend IDs changes. Uses a sorted-join cache key to avoid spurious re-runs.
- Store screen rewritten around the boost: removed the gift buttons; added a "Themes unlocked by a friend" callout for free users with `friendBoostActive`; subtitle and theme-section copy adapt to subscriber / boosted / free states.
- Settings premium card mentions the friend-share perk in both the subscriber and free-user copy; locked-theme alert and Browse-store links unchanged. Added a free-user hint when boost is active so the user understands why all their themes are suddenly unlocked.
- Added a Premium visual treatment for profile cards: a soft golden glow shadow on the polaroid card and a "PREMIUM" pill badge directly underneath it. `PremiumContext` now also exposes `premiumFriendIds: ReadonlySet<string>` (populated alongside `friendBoostActive` whenever `recheckFriendBoost` runs) and an `isUserPremium(userId)` helper that returns true for the signed-in user when subscribed or for any friend present in `premiumFriendIds`. `PeopleListItem` gained an optional `isPremium?: boolean` field. `PolaroidCarousel` reads `item.isPremium` and applies a `premiumGlow` shadow style + `premiumBadge` row (gold pill with star icon, "PREMIUM" text). `app/(app)/friends/index.tsx` enriches the people list with `isPremium` derived from `isUserPremium`. `app/(app)/wall/[authorId].tsx` paints the same glow + badge on the hero polaroid when the signed-in user (whose card is rendered as the hero) has Premium. Cross-device discovery still depends on the AsyncStorage-only friend probe in `recheckFriendBoost`; once a backend exposes friend premium status the badge will surface for all friends automatically.
- Replaced the friend-boost theme-sharing perk with a referral program. `PremiumContext` no longer exposes `friendBoostActive` and no longer flips `hasTheme` / `purchasedThemes` for friends of subscribers — only the user's own subscription unlocks the catalog now. New context surface: `referralCredits: number`, `referrerCode: string | null`, `applyReferralCode(code) => Promise<{ ok, error? }>`, plus a renamed `recheckPremiumFriends` (formerly `recheckFriendBoost`) that exists purely to populate `premiumFriendIds` for the gold-glow profile-card badge. New per-user storage keys: `yourfriends:premium:referralCredits:<uid>`, `yourfriends:premium:referrerCode:<uid>`, `yourfriends:premium:referralGranted:<uid>`. Cross-user message passing on the same device uses a global key `yourfriends:referral:pending:<UPPERCASED_FRIEND_CODE>` containing a JSON array of subscriber user IDs; on mount / `restore()` the provider drains that list under `currentUser.friendCode`, increments `referralCredits`, and clears it. `purchase()` deposits one entry into the referrer's pending list (marked one-time via `referralGranted`) so the credit cannot be replayed by repeated purchases. `applyReferralCode` is one-shot per user, refuses the user's own friend code, and stores the code uppercased. The old defunct `friendBoostKey` is best-effort removed for the current user on mount, and the rename `PremiumFriendBoostSync` -> `PremiumFriendsSync` was applied in `app/(app)/_layout.tsx`.
- Store screen rewritten: dropped the "Themes unlocked by a friend" callout and the "shares every theme with all your friends" copy. Added a "Refer a friend" SectionCard that shows the user's friend code, a Share button (uses `createFriendInviteLink` + RN Share) with referral copy, a gold "X free months earned" banner when `referralCredits > 0`, and a one-shot "Got referred? Enter the friend code" form (input + Apply, surfacing `applyReferralCode` errors). Settings screen copy updated to mention referrals instead of friend-of-premium gifting. New constant `REFERRAL_BONUS_LABEL = '1 free month'` exported from `PremiumContext`. Same-device caveat still applies: the pending-credit deposit lands in AsyncStorage on the subscriber's device, so the referrer only picks it up when both users have used the same device or once a backend (e.g. a Supabase `referrals` table that the IAP webhook writes to) is in place.
- Finalized the referral reward model around account creation instead of purchases: no general free trial; invite links/manual codes set an immutable referrer; self-referrals are rejected; referral rewards grant both the referrer and referee 7 days of Premium via `premium_until` when the Supabase RPC is available, with a local AsyncStorage fallback while the backend is being rolled out. Store/onboarding/settings/friend-limit copy now points at subscription Premium plus the referral reward instead of old free-trial/free-month language.
- Added referral unit coverage in `src/lib/__tests__/referrals.test.ts`: Premium expiry math, incoming referral-code storage/consume behavior, local Premium grant extension, empty/self/missing-code failures, immutable referrer enforcement, one-time reward enforcement, successful Supabase RPC reward mirroring, and the local 7-day fallback when the referral backend function is absent. Updated `src/features/social/__tests__/mappers.test.ts` so `rowToUser` now asserts `premiumUntil` mapping/defaults.
- Validation after the referral tests: `npm test -- --runInBand` passes all 5 suites / 53 tests, and `npm run typecheck` passes with `tsc --noEmit`.
- Added a private-notes skeleton for contact profiles: domain types, Supabase mappers/query keys/fetchers, SocialGraph CRUD helpers, repo schema tables/RLS/realtime entries, and a basic Profile/Notes pane on `app/(app)/profiles/contact/[contactId].tsx` with create/save/delete for text and link note blocks. Validation after this skeleton: `npm run typecheck` passes, VS Code reports no errors in the touched notes files, and `npm test -- --runInBand` passes all 5 suites / 53 tests. Photo-block upload and signed private-bucket rendering are still next-phase work.
- Added private-note functionality on contact profiles: image blocks now upload to the private `private_notes` Supabase bucket, render through signed URLs, can be removed individually, and are cleaned up when deleting the whole note; link blocks can be opened from the editor; note previews now show saved photo counts; and `supabase/schema.sql` records the required private bucket plus owner-scoped storage policies. Validation: `npm run typecheck`, `npm test -- --runInBand`, and VS Code diagnostics all pass.
- Troubleshooting note for private notes: if Supabase returns `Could not find the 'content' column` or `Could not find the 'type' column` of `contact_private_note_blocks` in the schema cache, the live table is missing the current block columns or PostgREST has not reloaded yet. Run the idempotent `alter table public.contact_private_note_blocks add column if not exists ...` repair SQL for `type`, `content`, `url`, `image_path`, `sort_order`, and `updated_at`, then run `select pg_notify('pgrst', 'reload schema');` before retrying the app.
- Migration wrinkle for private notes: if inserting a note fails with `block_type violates not_null constraint`, the live table still has an old required `block_type` column while the app writes `type`. Repair by copying `block_type` into `type` where needed and then dropping the stale `block_type` column, or at minimum dropping its NOT NULL constraint, followed by `select pg_notify('pgrst', 'reload schema');`.
- Updated the contact-profile private notes UX so the Notes pane behaves like a note index: tapping a note row or creating a new note now opens that note in a full-screen editor with its own Notes back control and Save action, while the list no longer renders the editor inline under the rows. Validation: VS Code diagnostics and `npm run typecheck` pass.
- Expanded private notes toward the iOS Notes-style freeform model: a note now supports multiple editable link blocks plus multiple photo blocks in the full-screen editor. Existing saved links render as editable rows with open/remove actions, a new add-link field can create additional link blocks, the Save action normalizes/persists all link rows, blank link rows are removed, and note previews show link counts when there are multiple links.
- Private-note photo blocks are free to add from the gallery; the Premium gallery gate no longer applies to adding photos inside a private note, while the existing broader profile/wall gallery gates remain unchanged.
- Refined the full-screen private-note editor into a single scrollable note body: the multiline text input no longer traps parent scrolling while the keyboard is open, the note screen gets extra bottom scroll padding, link/photo add controls sit inside the body canvas, saved links render as tappable in-body links, and photos render as full-width in-body image blocks. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Added the provided Supabase public env values to the local ignored `.env` and to the EAS production project environment, including `EXPO_PUBLIC_SUPABASE_KEY`, and updated `src/lib/supabase.ts` to accept `EXPO_PUBLIC_SUPABASE_KEY`, `EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, legacy anon keys, and matching Next-style fallbacks. Also made private-note photo uploads show a clearer message when the live `private_notes` Supabase Storage bucket is missing. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Corrected the private-note media bucket name from `private-notes` to the live Supabase bucket name `private_notes` in the upload helper and storage schema policies. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Made the private-note Storage policy repair idempotent by dropping/recreating the owner-scoped `private_notes` policies in `supabase/schema.sql`, and made photo upload RLS failures explain that the live bucket policies need to be applied. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Added `supabase/private_notes_storage_repair.sql` as a focused live-database repair script for the private-note photo RLS failure. Run it in Supabase SQL Editor; its verification queries should return one `private_notes` bucket row and four owner-folder Storage policies.
- Updated the private-note editor so the note body renders ordered blocks instead of a single text field above attachments: text inputs, tappable link blocks, and full-width photo blocks now live in one body stream, and adding a link or photo automatically inserts a fresh editable text block underneath so users can keep typing below media. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Updated private-note text blocks to auto-detect typed URLs like Apple Notes: normal text taps reopen the block for editing, detected URLs render inline with underline styling and open via `Linking.openURL`, and the separate paste-link composer no longer appears in the body toolbar. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Added Apple Notes-style auto-save to private notes: title, text blocks, and link blocks persist ~700ms after the last edit (and on editor close / note switch) via a debounced background writer that updates only changed blocks. The manual "Save" buttons were replaced with a small "Saving…/Saved" status indicator in the editor top bar; only "Delete" remains in the bottom action row. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Replaced the month-filter chips on someone's memory wall with a two-tab toggle: "{Name}'s wall" (default) shows their visible posts, "Shared wall" merges both connected users' visible posts chronologically. The full timeline now scrolls in one continuous list grouped by day; per-post author labels render correctly in shared mode. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Fixed two private-note auto-save bugs: (1) the title would slip back to its old/server value mid-typing because the hydration effect re-ran whenever a text/link block signature changed and unconditionally reset `noteTitleDraft`; now the title (and existing local drafts) are only seeded on note-switch, while newly-arriving server blocks still get drafts seeded. (2) the debounce useEffect's cleanup was calling `autoSavePrivateNote()` on every keystroke (because every draft was a dep), causing constant racing saves and apparent lag/deletes; cleanup now only clears the timer, and a separate effect flushes only on note switch / unmount. The auto-save callback also now reads its working state from a ref so the in-flight save (and the recursive pending re-run) always sees the latest keystrokes instead of a stale closure.
- Added a "Your wall / Shared wall" toggle to the contact profile (only shown when the contact is linked to a real friend). The shared wall lists only polaroids each of you has explicitly shared with the other (`visibility === 'visible_to_subject'` between you and the linked friend), sorted chronologically and grouped by day. The previous month-filter chips were removed for parity with the friend-wall screen. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Fixed the contact-profile shared-wall not actually merging the friend's posts about you: the previous query only fetched posts whose subject was you-the-contact or the linked-friend, which never includes posts authored by the friend with subject = current user. Now we additionally read `getVisiblePostsByAuthor(linkedUserId)` and union the friend's posts about you with your visible posts about them, deduped and sorted by `createdAt` desc. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Tightened the friend-profile shared wall so it no longer shows all of the current user's visible posts for other people. In `app/(app)/wall/[authorId].tsx`, the default wall is now only the viewed friend's visible posts whose subject is the current user, and Shared wall adds only the current user's visible posts whose subject is that viewed friend; the merged list is deduped and sorted by `createdAt` desc. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Made Shared wall the default and first tab on both wall surfaces: the friend-profile wall now opens on `Shared wall` before `{Name}'s wall`, and the contact profile opens on `Shared wall` before `Your wall` when the contact is linked. Unlinked contacts still fall back to the personal wall content because no shared wall can exist yet. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Added a personal Memory Wall section to `app/(app)/profiles/me.tsx`: it merges the current user's posts about people in their friends list (real users and saved contacts) with connected friends' `visible_to_subject` posts about the current user, dedupes by post id, sorts chronologically newest-first, and renders the same day-grouped polaroid timeline used by other wall surfaces. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Updated `src/components/WallPostCard.tsx` so all memory-card date labels include the year (e.g. `May 13, 2026`) and text-only note cards now render an author line under the note/date, matching photo polaroids' attribution. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Expanded the personal Memory Wall source in `app/(app)/profiles/me.tsx` so linked contact profiles read both their saved contact subject and their linked real-user subject before deduping. This prevents the user's own profile wall from missing polaroids that were migrated from contact-based posts to real-user posts. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Moved the date label above the body on text-only memory/note cards in `src/components/WallPostCard.tsx`, leaving the author attribution below the note. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Changed contact pin ordering so new pins append after existing pinned people instead of jumping to #1. Added `pinnedAt`/`pinned_at`, sorts pinned contacts by earliest pin timestamp first, writes `pinned_at` on pin and clears it on unpin, updates carousel prompt copy, and adds `supabase/pinned_contacts_order_repair.sql` for live DBs. Existing pinned contacts get `pinned_at = created_at` so future pins land after them. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Updated the home/friends screen floating bottom action from a single `+ Add Friend` pill into a three-action row: `Store`, primary `+ Add Friend`, and `Profile`, routing to `/(app)/store`, `/(app)/friends/add`, and `/(app)/profiles/me`. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Made the home/friends screen top and bottom safe-area bands transparent by adding an optional `safeAreaEdges` prop to `AppScreen` and opting the friends screen into left/right safe-area padding only. Floating top/bottom controls still position from device insets, but the scroll content can now show underneath instead of painting separate opaque bars. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Changed drawer destination routes for `Add Friend`, `Settings`, `Store`, and `Your Profile` from modal slide-up presentations to full-screen stack screens, and added in-screen Back buttons to each of those pages via `AppScreen` headers. Home remains the root screen. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Added a frosted tab-style background behind the home/friends bottom action buttons using `expo-blur`: the `Store`, `+ Add Friend`, and `Profile` buttons now sit inside a rounded translucent `BlurView` with a subtle tint and border. Added the `expo-blur` dependency. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Increased the home/friends frosted tab bar tint and blur intensity so the translucent background behind `Store`, `+ Add Friend`, and `Profile` reads more clearly over the carousel. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Removed friend-code sharing from My Profile and Settings, and removed premium-facing settings content so Settings only shows appearance and account controls. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Updated My Profile so the `Save Profile` footer only appears after the user changes their display name, profile photo, or profile facts. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Replaced the small Memory Wall `Add` link on contact profiles with two centered circular shortcuts for quick note and photo capture, matching the home shortcut style at a smaller size. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Made `AppScreen` edge-to-edge by default across the app so top and bottom safe-area bands no longer render as visible bars, while preserving inset spacing for headers, footers, and scroll content. The root status bar is explicitly transparent. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Cleaned up home carousel metadata so connected people only show the single `Connected` status row instead of also showing `Connected friend` below the active name. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Added a premium gallery photo source for Memory Wall posts: home/contact photo shortcuts now open a camera/gallery source picker, and the New Memory composer exposes `Camera` and `Gallery` buttons plus a `Change Photo` source picker. Non-premium gallery attempts still route through the existing gallery paywall. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Gallery photos added to Memory Wall posts now open the image cropper with a 1:1 aspect ratio from the home shortcut, contact profile shortcut, and New Memory composer so imported photos stay uniform with the polaroid frame style. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Restored relationship tags for contact profiles in the edit screen with preset/custom tag controls, but tags are kept off all polaroid-style cards and previews. Validation: `npm run typecheck` and `npm test -- --runInBand` pass.
- Added premium AI caption generation for photo memories: the New Memory composer now has tone chips and an `AI Caption` action that sends the compressed photo plus selected relationship context to a Supabase Edge Function, which verifies Premium before calling OpenAI. Added Edge Function secret docs for `OPENAI_API_KEY` and `OPENAI_CAPTION_MODEL`. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Deployed the `generate-ai-caption` Supabase Edge Function to project `lxprnlhffkdsecfrvodv`; `supabase functions list` shows it ACTIVE at version 1, and `supabase secrets list` confirms the required `OPENAI_API_KEY` secret is configured.
- Improved AI caption failure handling after the live Edge Function returned Supabase's generic non-2XX wrapper: `generateAiCaptions` now reads JSON error bodies from `FunctionsHttpError`, and `PremiumContext` syncs existing local Premium access back to `profiles.premium_until` so server-side Premium verification can pass. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Redeployed `generate-ai-caption` after the live OpenAI call returned the generic "Could not write captions right now" message. The Edge Function now returns a sanitized OpenAI status/detail message, expected OpenAI/no-caption failures return JSON with an `error` field so older app builds can display it, `OPENAI_CAPTION_MODEL` is explicitly set to `gpt-4o-mini`, and `supabase functions list` shows the function ACTIVE at version 4.
- Tuned AI captions to be photo-first and much less fact-driven: the Edge Function now tells OpenAI to ground captions in visible image details, treats relationship context as subtle background flavor only, sends high-detail image input, and caps facts/notes/previous memories more aggressively. Redeployed `generate-ai-caption`; Supabase shows it ACTIVE at version 5.
- Switched the deployed AI caption model secret to `OPENAI_CAPTION_MODEL=gpt-5-mini` for a stronger production default than `gpt-4o-mini`; verified the secret is present with `supabase secrets list`.
- Added premium AI caption generation to the Edit Memory screen for existing photo polaroids: edit mode now has the same tone chips, `AI Caption` action, and tappable suggestions as the New Memory composer. The shared AI caption helper now downloads remote image URLs before compression so already-uploaded polaroids can be captioned. Validation: VS Code diagnostics, `npm run typecheck`, and `npm test -- --runInBand` pass.
- Fixed `gpt-5-mini` compatibility for AI captions after OpenAI rejected `max_tokens`: the Edge Function now uses `max_completion_tokens` for GPT-5/o-series style models and keeps `max_tokens` for older chat models. Redeployed `generate-ai-caption`; Supabase shows it ACTIVE at version 7.
- Fixed the next `gpt-5-mini` AI caption compatibility issue after OpenAI rejected custom `temperature: 0.9`: the Edge Function now omits temperature for GPT-5/o-series style models while keeping `0.9` for older chat models. Redeployed `generate-ai-caption`; Supabase shows it ACTIVE at version 8.
- Hardened AI captions after `gpt-5-mini` returned a successful response with no parsed captions: GPT-5/o-series calls now get a larger `max_completion_tokens` budget, response parsing handles text arrays and fenced JSON, and empty parses log finish reason/content preview for debugging. Redeployed `generate-ai-caption`; Supabase shows it ACTIVE at version 9.
- Restored relationship tags as profile-only metadata: contact profile screens now show saved relationship tags as chips below the hero/status area, while polaroid cards and wall cards remain tag-free.
- Expanded the contact profile relationship-tag preset list from a small starter set to a broader default library across friendship, family, romance, school/work, distance, activities, and shared-life labels. The edit-profile tag chooser now wraps presets in a grid for easier scanning while still limiting saved tags to 6.
- Tightened relationship-tag presets so defaults are actual relationship/how-you-know-them labels only, such as `Friend`, `New Friend`, `Online Friend`, `Family`, `Partner`, `Coworker`, and `Classmate`; removed vibe/activity-style presets like `Golden Hour`, `Coffee Runs`, and `Concerts`.
- Moved the contact edit relationship-tag editor to the bottom of the profile editor, below profile theme controls, and restored the preset relationship tags to a left-right horizontal scroller.
- Increased relationship-tag influence for AI captions: the Edge Function now treats tags as a high-priority emotional lens for wording, closeness, and humor while keeping the photo as the source of visual details. Expanded AI caption themes beyond Funny/Romantic/Sweet/Nostalgic/Casual with Heartfelt, Playful, Poetic, Hype, Cozy, Sassy, Deadpan, Cinematic, Inside Joke, and Flirty. Redeployed `generate-ai-caption`; Supabase shows it ACTIVE at version 10.
- Made AI captions more witty by adding a dedicated `Witty` theme, making it the default selected tone in New Memory and Edit Polaroid, and tuning the Edge Function prompt to prefer crisp wit, specific observations, and clever relationship-aware wording over generic lines. Redeployed `generate-ai-caption`; Supabase shows it ACTIVE at version 11.
- Enforced owner-only polaroid editing: contact profile edit mode only opens `/memories/edit` for posts authored by the current user, other people's polaroids are disabled in edit mode, the edit route redirects away from non-owned posts, and shared wall-post update/delete mutations now guard against non-owner changes.
- Moved contact profile Memory Wall controls into a sticky blurred bottom bar: note/photo shortcuts and the Shared wall/Your wall toggle now float at the bottom with safe-area padding, matching the home screen's frosted control style, while the inline Memory Wall section keeps only the heading and wall content.
- Replaced the native photo-source action sheet with a custom app-level `PhotoSourceSheetHost`: `showPhotoSourceSheet` now opens a frosted liquid-glass style modal with blur, translucent tint layers, rounded glass buttons, and backdrop-tap dismissal for Take Photo / Choose from Gallery flows.
- Made the contact profile Memory Wall control bar dockable: dragging the expanded frosted bar collapses it into a draggable glass ball that can be moved around the screen, and tapping the ball restores the expanded controls at the bottom.
- Smoothed the dockable Memory Wall control interaction: the expanded bar now follows the drag, collapses through an animated crossfade/scale into the glass ball instead of snapping, the ball can be dragged smoothly, release velocity projects into a springy settle, and tapping the ball animates it back down before expanding the bottom controls.
- Changed the docked Memory Wall glass ball release from spring-to-point settling to puck-style fling physics: release velocity now carries the ball forward, friction gradually slows it, and it bounces off screen edges instead of snapping/sticking to a projected spot.
- Adjusted the expanded-to-ball Memory Wall transition so puck physics starts immediately at release: the ball now appears at the bar's release point and begins its inertial fling while the morph/fade animation is still happening, instead of waiting until the ball state is fully visible.
- Added Back, Pin/Unpin, and Edit/Done icon controls to the expanded Memory Wall glass ball menu, with extra bottom padding so the taller menu does not cover wall content.
- Tuned the Memory Wall floating control so it starts as the draggable ball by default, loses much more velocity when it hits screen edges, and uses lighter/more transparent glass fills for both the ball and expanded menu.
- Compactified the expanded Memory Wall ball menu into one side-by-side action row: Back now uses a real back-label style, Edit/Done uses the actual pill-button style, Pin remains a compact icon button, note/photo sit beside them, and puck movement now has much heavier overall friction with a lower fling speed cap.
- Rebalanced the expanded Memory Wall ball menu so Note and Photo are the large central focal actions, while Back, Pin/Unpin, and Edit/Done are smaller supporting controls in the same compact row.
- Simplified the expanded Memory Wall ball menu back to only the primary Note and Photo actions, and added a top-bar Notifications button with unread badge immediately to the left of the Pin control.
- Moved the contact profile Shared wall / Your wall toggle back into the inline Memory Wall section under the heading, removed it from the expanded ball menu, and tightened the expanded menu bottom padding now that the ball menu only contains Note and Photo.
- Cleaned up the Memory Wall floating menu now that it only has Note and Photo: the expanded glass pod opens by default, shrink-wraps around the two buttons instead of spanning the screen, and uses a lighter background/drag handle.
- Disabled the Memory Wall menu's ball/puck transformation path and replaced it with a scroll-triggered compact Note/Photo pod: the menu is hidden near the top, pops up while scrolling down, and hides again on upward scroll.
- Reversed the Memory Wall Note/Photo pod scroll behavior and default state: it is open by default, remains visible near the top, hides while scrolling down, and pops back in on upward scroll.
- Added the Premium calendar/reminders first pass: new `calendar_events` Supabase schema/RLS/realtime entry, calendar domain types, data/query/reminder/provider layer, a full calendar screen with free read-only memory/event viewing and Premium-gated add/edit/delete/local reminders, home/drawer navigation, and updated store/paywall copy. Validation: `npm run typecheck`, VS Code diagnostics, and `npm test -- --runInBand` pass.
- Refined calendar event rows so birthdays/events show who they are for/with, added per-occurrence completion via `completed_occurrence_keys`, and made completed occurrences skip local reminder scheduling. Validation: `npm run typecheck`, VS Code diagnostics, and `npm test -- --runInBand` pass.
- Fixed a calendar crash from stale cached event objects missing `completedOccurrenceKeys` by defaulting completion/reminder arrays at runtime in the provider, reminder scheduler, and calendar screen. Validation: `npm run typecheck`, VS Code diagnostics, and `npm test -- --runInBand` pass.
